-- Schema:
-- users (id, name, email, created_at)
-- orders (id, user_id, total, created_at)
-- products (id, name, price)
-- order_items (order_id, product_id, quantity)

-- Queries a escribir:
-- 1. Total gastado por cada usuario


-- 2. Productos más vendidos este año


-- 3. Usuarios que no compraron en los últimos 6 meses

